import * as styled from './styles';
import Sidebar from '../Sidebar';


const Root = () => {
    return (
        <styled.Wrapper>
            <Sidebar/>
        </styled.Wrapper>
    );
};

export default Root;